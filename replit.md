# HAB Academy — Replit Agent Guardrails (read first, mandatory)
1. GitHub (heath-prog/hab-academy) is the SINGLE SOURCE OF TRUTH. Code is written by Claude (Cowork) and pushed here. Your job: pull, run, test, report.
2. DO NOT modify code, content, or config unless the user EXPLICITLY asks in this chat. Do NOT reconstruct "missing" files — server.js and all content exist in the repo; if something looks missing, PULL first. Never fix proactively; describe and stop.
3. Never commit/push without instruction. Never touch Secrets or the data/ directory.
4. When asked to test: run, exercise flows (login, /library, /curriculum, roles), report errors verbatim.
5. On pull conflicts with your local edits: discard local changes — GitHub wins.
